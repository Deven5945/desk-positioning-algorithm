# 자리이동 최적화

5행 6열 교실에서 자리이동 시뮬

## 실행

`index.html` 열거나, 프로젝트 폴더에서 로컬 웹 서버를 실행

```powershell
python -m http.server 8000
```

`http://localhost:8000` 열기
## 모델 규칙

- Fisher–Yates 셔플로 배치
- 책상 사이의 통로를 거쳐 이동
- 한 단계에서 같은 칸을 차지할 수 없고, 맞바꾸기 이동은 금지
- 세 칸 이상이 동시에 비워지는 순환 이동은 허용
